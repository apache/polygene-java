jrubyVersion = '1.5.6'

dependencies {
  compile(project(':libraries:scripting:common'))
  compile(project(":core:api"))
  compile(project(":core:bootstrap"))
  compile(libraries.slf4j_api)
  compile('org.jruby:jruby:' + jrubyVersion )
  testCompile(libraries.junit)
  testCompile(project(":core:testsupport"))
  testRuntime(project(":core:spi"))
  testRuntime(project(":core:runtime"))
  testRuntime(libraries.asm)
  testRuntime(libraries.asm_commons)
  testRuntime(libraries.slf4j_simple)
}

jar {
  manifest {
    symbolicName = 'org.qi4j.scripting.jruby'
    description = "Qi4j JRuby Library allows for Mixins to be implemented in JRuby scripts instead of directly in compiled Java."
    name = "Qi4j Library - Scripting - JRuby"
    instruction 'Export-Package', 'org.qi4j.scripting.jruby;version=' + version
    instruction 'Import-Package', '*'
  }
}