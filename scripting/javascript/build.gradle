javascriptVersion = '1.7R2'

dependencies {
  compile(project(':scripting:common'))
  compile(libraries.qi4j_core_api)
  compile(libraries.qi4j_core_bootstrap)
  compile( 'rhino:js:'+ javascriptVersion )
  compile(libraries.slf4j_api)
  testCompile(libraries.junit)
  testRuntime(libraries.asm)
  testRuntime(libraries.asm_commons)
  testRuntime(libraries.qi4j_core_spi)
  testRuntime(libraries.slf4j_simple)
  testCompile(libraries.qi4j_core_testsupport)
  testRuntime(libraries.qi4j_core_runtime)
}

jar {
  manifest {
    symbolicName = 'org.qi4j.scripting.javascript'
    description = "Qi4j JavaScript Library allows for Mixins to be implemented in JavaScript instead of directly in compiled Java."
    name = "Qi4j Library - Scripting - JavaScript"
    instruction 'Export-Package', 'org.qi4j.scripting.javascript;version=' + version
    instruction 'Import-Package', '*'
  }
}